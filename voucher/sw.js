// generated by scripts/build-site.js — do not edit
const CACHE = 'vbb-voucher-0.4.2-f8cc8d96';
const PRECACHE = [
  "./",
  "./index.html",
  "./manifest.webmanifest",
  "./icon-192.png",
  "./icon-512.png",
  "../shared/vbb-crypto.js",
  "../shared/vouchtoon.js",
  "../shared/vendor/circomlibjs.esm.js",
  "../shared/vendor/buffer.esm.js",
  "../shared/vendor/face-api.min.js",
  "../shared/vendor/weights/face_landmark_68_tiny_model-shard1",
  "../shared/vendor/weights/face_landmark_68_tiny_model-weights_manifest.json",
  "../shared/vendor/weights/tiny_face_detector_model-shard1",
  "../shared/vendor/weights/tiny_face_detector_model-weights_manifest.json"
];

self.addEventListener('install', e => {
  e.waitUntil(caches.open(CACHE).then(c => c.addAll(PRECACHE)).then(() => self.skipWaiting()));
});

self.addEventListener('activate', e => {
  e.waitUntil(
    caches.keys()
      .then(keys => Promise.all(keys.filter(k => k.startsWith('vbb-voucher-') && k !== CACHE).map(k => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});

self.addEventListener('fetch', e => {
  if (e.request.method !== 'GET') return;
  e.respondWith(
    caches.match(e.request, { ignoreSearch: true }).then(hit => hit || fetch(e.request).then(resp => {
      if (resp.ok && new URL(e.request.url).origin === location.origin) {
        const copy = resp.clone();
        caches.open(CACHE).then(c => c.put(e.request, copy));
      }
      return resp;
    }))
  );
});
